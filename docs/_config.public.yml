# Deprecated configuration file.
# Public docs builds should use docs/_config.yml and front-matter nav controls.
# This file is kept only for backward compatibility with older local scripts.

exclude:
  - _config.public.yml
