// Package web owns the browser-facing onboarding and campaign entry UX.
//
// It translates authenticated identity + routing context into the same campaign,
// participant, and character view models used by API and MCP adapters, keeping
// the domain language consistent across UI and service layers.
package web
