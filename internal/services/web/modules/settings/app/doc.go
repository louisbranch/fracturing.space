// Package app contains settings domain contracts and orchestration logic.
//
// The settings root package owns HTTP transport concerns (module wiring,
// handlers, and routes). This package owns user-scoped validation and
// mutation/read orchestration independent of transport.
package app
