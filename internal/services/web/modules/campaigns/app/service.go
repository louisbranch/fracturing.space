package app

// This boundary anchor file intentionally remains present because module
// architecture tests assert the layered `app/service.go` path. Service
// contracts and orchestration are decomposed into focused `service_*.go` files.
