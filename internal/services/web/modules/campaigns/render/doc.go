// Package render owns section-specific campaign detail and character-creation
// render models plus module-owned markup, while reusing only shared shell
// primitives such as images and timestamps.
package render
