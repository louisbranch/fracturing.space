// Package characters owns campaign character transport routes, including the
// dedicated character-creation workflow surface.
//
// It consumes shared workspace-shell support from `campaigns/detail` and keeps
// character-specific view mapping, form parsing, and workflow redirects local
// to the character surface.
package characters
