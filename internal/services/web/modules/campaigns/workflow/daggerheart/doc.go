// Package daggerheart owns the Daggerheart-specific character-creation
// workflow implementation used by the campaigns web module.
//
// Start here when changing system-specific creation form parsing, catalog-to-
// view mapping, or page-model assembly for Daggerheart. The parent
// `campaigns/workflow` package owns transport-facing workflow orchestration;
// this package should stay on Daggerheart-specific parsing and view logic only.
package daggerheart
