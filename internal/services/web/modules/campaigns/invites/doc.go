// Package invites owns campaign invite transport routes.
//
// It consumes shared workspace-shell support from `campaigns/detail` and keeps
// invite-specific JSON contracts, form parsing, and view mapping local to the
// invite surface.
package invites
