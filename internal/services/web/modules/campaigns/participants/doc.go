// Package participants owns campaign participant transport routes.
//
// It consumes shared workspace-shell support from `campaigns/detail` and keeps
// participant-specific form parsing and view mapping local to the participant
// surface.
package participants
