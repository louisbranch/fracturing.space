// Package discovery owns unauthenticated discovery transport routes and the
// module-facing seam into discovery page orchestration.
//
// Transport stays in the root package. Area-local orchestration lives in
// `discovery/app`, and backend protocol mapping lives in `discovery/gateway`.
package discovery
