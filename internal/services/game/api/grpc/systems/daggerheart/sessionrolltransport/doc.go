// Package sessionrolltransport owns the low-level Daggerheart session gameplay
// roll transport surface.
//
// It handles action rolls, damage rolls, adversary attack rolls, and
// adversary action checks behind explicit read-store contracts and specialized
// write callbacks so the root Daggerheart package can stay a thin facade.
package sessionrolltransport
