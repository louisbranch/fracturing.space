// Package systems contains system-specific gRPC transport packages.
//
// Each subdirectory implements the transport layer for one game system,
// bridging proto requests to domain commands through shared write infrastructure.
package systems
