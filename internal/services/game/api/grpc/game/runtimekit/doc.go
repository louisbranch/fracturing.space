// Package runtimekit provides deterministic runtime/bootstrap helpers for game
// gRPC transport tests. It keeps write-runtime setup and clock/ID fixtures out
// of the store-fake package so contributors can depend on the exact helper
// surface they need.
package runtimekit
