// Package eventtransport implements the gRPC transport layer for the event
// entity. It owns the EventService handler, application logic, timeline
// projection, and proto mappers.
package eventtransport
