// Package normalize provides generic string and ID normalization helpers,
// centralizing whitespace trimming so callers use a single vocabulary
// instead of scattered strings.TrimSpace calls.
package normalize
