// Package payload contains all Daggerheart command/event payload types
// and event type constants. These types are shared across the decider,
// folder, adapter, and validator sub-packages.
//
// The root daggerheart package re-exports all public symbols via type
// aliases and constant aliases so external consumers are unaffected.
package payload
